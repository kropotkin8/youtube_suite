# Database (SQLAlchemy URL)
DATABASE_URL=postgresql+psycopg2://cip:cip@localhost:5432/creator_intel

# YouTube Data API v3
YT_API_KEY=

# OpenAI (descriptions)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1

# FFmpeg binary
FFMPEG_PATH=ffmpeg

# Shorts stack (optional extras)
HF_TOKEN=
WHISPER_MODEL=small
WHISPERX_DEVICE=cpu
DIARIZATION_DEVICE=cpu
MIN_CLIP_DURATION=5.0
MAX_CLIP_DURATION=30.0
TARGET_CLIP_DURATION=15.0
NUM_CLIPS=5
SCORE_SEMANTIC=0.4
SCORE_ENERGY=0.2
SCORE_SPEAKER_CHANGE=0.15
SCORE_KEYWORD=0.15
SCORE_SENTIMENT=0.1
EMBEDDING_MODEL=all-mpnet-base-v2
AUDIO_SAMPLE_RATE=16000
AUDIO_CHANNELS=1

# Storage root (uploads, clips, audio)
CIP_DATA_DIR=./data
